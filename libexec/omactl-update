#!/usr/bin/env bash

# Update: system-wide update orchestration

set -euo pipefail

# ----------------------------
# Core actions
# ----------------------------
test() {
  echo "test"
}

# ----------------------------
# Completion
# ----------------------------
__complete() {
  local comp_words=("$@")
  local cmd="${comp_words[0]:-}"
  local cur="${comp_words[-1]:-}"

  case "$cmd" in
  test)
    ;;

  *)
    compgen -W "test" -- "$cmd"
    ;;
  esac
}

# ----------------------------
# Dispatcher
# ----------------------------
case "${1:-}" in
test)
  test
  ;;

__complete)
  shift
  __complete "$@"
  ;;

*)
  echo "usage: update test" >&2
  exit 1
  ;;
esac
