#!/bin/bash

# Launch a floating terminal with the Omadora presentation wrapper

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omadora.terminal --title=Omadora -e bash -c "omadora-show-logo; $cmd; if (( \$? != 130 )); then omadora-show-done; fi"
