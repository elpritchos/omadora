#!/bin/bash

# shellcheck source=lib/common/init.sh
source "$(dirname "${BASH_SOURCE[0]}")/../lib/common/init.sh"

# Launch the audio controls TUI (provided by wiremix).

omadora_exec omadora-launch-or-focus-tui wiremix
