#!/bin/bash

# Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/omadora/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! omadora-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
