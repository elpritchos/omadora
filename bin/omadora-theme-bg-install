#!/bin/bash

CURRENT_THEME_NAME=$(cat "$HOME/.config/omadora/current/theme.name")
THEME_USER_BACKGROUNDS="$HOME/.config/omadora/backgrounds/$CURRENT_THEME_NAME"

mkdir -p "$THEME_USER_BACKGROUNDS"
nautilus "$THEME_USER_BACKGROUNDS"
